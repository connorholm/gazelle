<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
<% if user_signed_in? %>
    <%# <%= @data['weather'].first["main"] %>
    <br/>

    <%= form_tag(home_index_path, method: "get") do %>
        <%= label_tag("Enter City:") %>
        <%= text_field_tag(:city) %>
        <%= submit_tag "Search", :name => nil %>
    <% end %>
    <%= @data %>
    
<% end %>
